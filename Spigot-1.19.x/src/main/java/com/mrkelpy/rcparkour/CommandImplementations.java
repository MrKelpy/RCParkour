package com.mrkelpy.rcparkour;

import com.mrkelpy.rcparkour.commons.commands.ICommandImplementations;

/**
 * This class holds all the VERSION-SPEFICIC command implementations; It still needs to exist
 * just to hold the version-unspecific ones, present as defaults in ICommandImplementations.
 */
public class CommandImplementations implements ICommandImplementations { }

